"""Tests for photon.core.photometry — placeholder."""

from __future__ import annotations

# TODO: implement photometry tests in a future milestone.
# Suggested coverage:
# - run_aperture_photometry returns one result dict per position
# - flux values are finite for a synthetic Gaussian source
# - flux_err is positive
# - PhotometryError raised for non-2D input
# - PhotometryError raised when photutils is unavailable (monkeypatch import)

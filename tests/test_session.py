"""Tests for photon.core.session — placeholder."""

from __future__ import annotations

# TODO: implement session tests in a future milestone.
# Suggested coverage:
# - PhotonSession.add_frame appends to session.frames
# - PhotonSession.clear resets all fields
# - PhotonSession.frame_count returns correct count
# - PhotonSession.is_plate_solved returns False when frames lack WCS
